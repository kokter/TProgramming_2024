def move_zeros(lst):
    result = []
    
    zero_count = 0
    
    for num in lst:
        if num != 0:
            result.append(num)
        else:
            zero_count += 1
    
    result.extend([0] * zero_count)
    
    return result